---
import ConatactCard from "./ContactCard.astro";
const contactsInfo = [
  {
    link: "mailto:mahdebusines123@gmail.com",
    text: "📧 Email",
  },
  {
    link: "https://github.com/Mahdi-abu-hasan",
    text: "💻 GitHub",
  },
  {
    link: "https://www.linkedin.com/in/mahde-abu-alhasan-090a82286",
    text: "💼 LinkedIn",
  },
];
---

<section id="contact">
  <h2>Get In Touch</h2>
  <div class="contact-content">
    <p class="contact-description">
      I'm always open to discussing new projects, creative ideas, or
      opportunities to be part of your visions.
    </p>
    <div class="contact-links">
      {
        contactsInfo.map((contact) => (
          <ConatactCard link={contact.link} text={contact.text} />
        ))
      }
    </div>
  </div>
</section>

<style>
  #contact {
    max-width: 800px;
    margin: 0 auto 70px;
    padding: 6rem 2rem;
    text-align: center;
  }

  h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 2rem;
    background: linear-gradient(
      135deg,
      var(--text-primary),
      var(--accent-light)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    align-items: center;
  }

  .contact-description {
    color: var(--text-secondary);
    font-size: 1.2rem;
    line-height: 1.8;
    max-width: 600px;
  }

  .contact-links {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  @media (max-width: 768px) {
    #contact {
      padding: 4rem 1.5rem;
    }

    .contact-links {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
    }

    .contact-description {
      font-size: 1.1rem;
    }
  }
</style>
